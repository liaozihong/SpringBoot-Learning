server:
  port: 9091
spring:
  application:
    name: local-grpc-client
grpc:
  client:
    local-grpc-server:
      host:
        - ${LOCAL-GRPC-HOST:127.0.0.1}
      port:
        - 9000
      enableKeepAlive: true
      keepAliveWithoutCalls: true